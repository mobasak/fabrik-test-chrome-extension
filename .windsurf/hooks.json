{
  "description": "Windsurf Cascade hooks for Fabrik enforcement",
  "hooks": [
    {
      "event": "post_write_code",
      "command": "python3 -m scripts.enforcement.validate_conventions --strict --git-diff",
      "description": "Enforce Fabrik coding standards after writing (validates changed + untracked files)",
      "timeout": 15,
      "cwd": "/opt/fabrik-test-chrome-extension"
    },
    {
      "event": "post_write_code",
      "command": "python3 -m scripts.enforcement.check_secrets",
      "description": "Scan for hardcoded secrets after writing",
      "timeout": 10,
      "cwd": "/opt/fabrik-test-chrome-extension"
    }
  ]
}
